---
parameter_space:
  # ... Run defaults ...........................................................
  run: &run_defaults
    seed: 42
    num_steps: 1
    write_every: 1

  <<: *run_defaults
  KronGen:
    NetworkAnalyser:
      <<: *run_defaults
